-- create Table Categories(
-- CategoryId serial primary key,
-- CategoryName varchar(100) not null
-- 

--select *from "ToDoLists";

-- create table ToDoLists(
-- ToDoListId serial primary key,
-- Title varchar(200) not null,
-- Description varchar(1000),
-- Status bit,
-- Priority varchar(50),
-- CategoryId int,
--   Foreign key (CategoryId) references Categories(CategoryId) on delete cascade   
-- )


--insert into Categories(CategoryName) values ('İş')
--insert into Categories(CategoryName) values ('Eğitim')
--insert into Categories(CategoryName) values ('Spor')
--insert into Categories(CategoryName) values ('Toplantı')
--insert into Categories(CategoryName) values ('Aktivite')